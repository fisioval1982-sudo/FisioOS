# Language & Content Authority Contract

## Principle

Default output is es-ES. Reference text never determines output language or final copy. Visible clinical text must resolve from approved content IDs. Renderer must not translate, paraphrase, summarize, expand, complete or invent clinical copy.

## Failure behavior

A violated blocking constraint prevents canonical rendering. No creative fallback is permitted.
