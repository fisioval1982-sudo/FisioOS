# Render QA & Acceptance Contract

## Principle

Canonical output must pass automated checks for language, content traceability, complete text, overflow/overlap, typography minima, assets, anatomy, dimensions and approved components/layouts/tokens. Human QA is required when configured for anatomy, claim-sensitive diagrams or new visual primitives.

## Failure behavior

A violated blocking constraint prevents canonical rendering. No creative fallback is permitted.
