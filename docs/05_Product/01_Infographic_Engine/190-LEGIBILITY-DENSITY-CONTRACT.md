# Legibility & Density Contract

## Principle

Output profiles must define approved minimum typography, line height, safe areas, contrast, density and overflow behavior. Never shrink below approved minima, clip, hide, overlap or silently remove text. If content does not fit, use an approved alternate layout or fail.

## Failure behavior

A violated blocking constraint prevents canonical rendering. No creative fallback is permitted.
