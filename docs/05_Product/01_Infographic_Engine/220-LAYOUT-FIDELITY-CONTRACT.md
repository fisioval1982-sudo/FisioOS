# Layout Fidelity Contract

## Principle

Canonical rendering requires explicit canvas/profile, region order, geometry/grid placement, alignment, padding, gaps, component order, occupancy and overflow policy. Descriptions such as clean, balanced or two-column are not sufficient render instructions.

## Failure behavior

A violated blocking constraint prevents canonical rendering. No creative fallback is permitted.
